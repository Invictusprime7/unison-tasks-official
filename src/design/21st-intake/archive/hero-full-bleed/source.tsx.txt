{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "content": [
      {
        "type": "text",
        "text": "# Hero \u2014 default.tsx\nHero Section \u2013 Aurora Background\nA visually immersive hero section with a smooth aurora-style animated background. Enhances first impressions with subtle motion while supporting both light and dark themes. Ideal for landing pages, portfolios, and apps that need a captivating entry point.\n\ninstall: npx shadcn@latest add \"https://21st.dev/r/ravikatiyar162/hero-2?api_key=$API_KEY_21ST\"\n\n## Component\n```tsx\n\"use client\";\n\nimport React from \"react\";\nimport { motion } from \"framer-motion\";\nimport { cn } from \"@/lib/utils\";\n\ninterface AuroraHeroProps {\n  children: React.ReactNode;\n  className?: string;\n}\n\n/**\n * Injects the CSS keyframes for the aurora animation.\n */\nconst AuroraAnimation = () => (\n  <style>\n    {`\n      @keyframes aurora-1 {\n        0% { transform: translate(0%, 0%) scale(1); }\n        25% { transform: translate(20%, -20%) scale(1.2); }\n        50% { transform: translate(-20%, 20%) scale(0.8); }\n        75% { transform: translate(10%, -10%) scale(1.1); }\n        100% { transform: translate(0%, 0%) scale(1); }\n      }\n      @keyframes aurora-2 {\n        0% { transform: translate(0%, 0%) scale(1); }\n        25% { transform: translate(-20%, 20%) scale(1.1); }\n        50% { transform: translate(20%, -20%) scale(0.9); }\n        75% { transform: translate(-10%, 10%) scale(1.2); }\n        100% { transform: translate(0%, 0%) scale(1); }\n      }\n    `}\n  </style>\n);\n\nexport const AuroraHero = ({ children, className }: AuroraHeroProps) => {\n  return (\n    <div className=\"h-full w-full\">\n      <AuroraAnimation />\n      <div\n        className={cn(\n          \"relative flex h-screen w-full flex-col items-center justify-center overflow-hidden rounded-md bg-background antialiased\",\n          className\n        )}\n      >\n        {/* The Aurora Background */}\n        <div className=\"absolute inset-0 z-0\">\n          {/* Main Aurora Blob 1 (Primary Color) */}\n          <div className=\"absolute -top-1/4 left-1/4 h-96 w-96 animate-[aurora-1_20s_ease-in-out_infinite] rounded-full bg-primary/30 opacity-20 blur-3xl filter dark:opacity-50\" />\n          {/* Main Aurora Blob 2 (Secondary/Muted Color) */}\n          <div className=\"absolute -bottom-1/4 right-1/4 h-96 w-96 animate-[aurora-2_20s_ease-in-out_infinite] rounded-full bg-muted-foreground/30 opacity-10 blur-3xl filter dark:opacity-30\" />\n        </div>\n\n        {/* Content */}\n        <div className=\"relative z-10\">{children}</div>\n      </div>\n    </div>\n  );\n};\n```\n\n## Demo (usage)\n```tsx\n\"use client\";\n\nimport React from \"react\";\nimport { motion } from \"framer-motion\";\nimport { AuroraHero } from \"@/components/ui/hero-2\"; // Adjust the import path as needed\n\nexport default function AuroraHeroDemo() {\n  return (\n    <AuroraHero>\n      <motion.h1\n        initial={{ opacity: 0, y: 40 }}\n        animate={{ opacity: 1, y: 0 }}\n        transition={{\n          delay: 0.3,\n          duration: 0.8,\n          ease: \"easeInOut\",\n        }}\n        className=\"bg-gradient-to-b from-foreground to-muted-foreground bg-clip-text text-center text-4xl font-bold tracking-tight text-transparent md:text-6xl\"\n      >\n        Experience the Aurora\n      </motion.h1>\n      <motion.p\n        initial={{ opacity: 0, y: 40 }}\n        animate={{ opacity: 1, y: 0 }}\n        transition={{\n          delay: 0.5,\n          duration: 0.8,\n          ease: \"easeInOut\",\n        }}\n        className=\"mx-auto mt-4 max-w-lg text-center text-base text-muted-foreground\"\n      >\n        A stunning, animated background that brings your hero sections to\n        life, adapting beautifully to both light and dark themes.\n      </motion.p>\n    </AuroraHero>\n  );\n}\n```"
      }
    ],
    "structuredContent": {
      "component": {
        "id": 6834,
        "componentId": 4681,
        "authorUserId": "user_2zMtrqo9RMaaIn4f8F2z3oeY497",
        "name": "Hero",
        "demoName": "default.tsx",
        "description": "Hero Section \u2013 Aurora Background\nA visually immersive hero section with a smooth aurora-style animated background. Enhances first impressions with subtle motion while supporting both light and dark themes. Ideal for landing pages, portfolios, and apps that need a captivating entry point.",
        "previewUrl": "https://cdn.21st.dev/ravikatiyar162/hero-2/default/preview.1756997669048.png",
        "installCommand": "npx shadcn@latest add \"https://21st.dev/r/ravikatiyar162/hero-2?api_key=$API_KEY_21ST\"",
        "componentCode": "\"use client\";\n\nimport React from \"react\";\nimport { motion } from \"framer-motion\";\nimport { cn } from \"@/lib/utils\";\n\ninterface AuroraHeroProps {\n  children: React.ReactNode;\n  className?: string;\n}\n\n/**\n * Injects the CSS keyframes for the aurora animation.\n */\nconst AuroraAnimation = () => (\n  <style>\n    {`\n      @keyframes aurora-1 {\n        0% { transform: translate(0%, 0%) scale(1); }\n        25% { transform: translate(20%, -20%) scale(1.2); }\n        50% { transform: translate(-20%, 20%) scale(0.8); }\n        75% { transform: translate(10%, -10%) scale(1.1); }\n        100% { transform: translate(0%, 0%) scale(1); }\n      }\n      @keyframes aurora-2 {\n        0% { transform: translate(0%, 0%) scale(1); }\n        25% { transform: translate(-20%, 20%) scale(1.1); }\n        50% { transform: translate(20%, -20%) scale(0.9); }\n        75% { transform: translate(-10%, 10%) scale(1.2); }\n        100% { transform: translate(0%, 0%) scale(1); }\n      }\n    `}\n  </style>\n);\n\nexport const AuroraHero = ({ children, className }: AuroraHeroProps) => {\n  return (\n    <div className=\"h-full w-full\">\n      <AuroraAnimation />\n      <div\n        className={cn(\n          \"relative flex h-screen w-full flex-col items-center justify-center overflow-hidden rounded-md bg-background antialiased\",\n          className\n        )}\n      >\n        {/* The Aurora Background */}\n        <div className=\"absolute inset-0 z-0\">\n          {/* Main Aurora Blob 1 (Primary Color) */}\n          <div className=\"absolute -top-1/4 left-1/4 h-96 w-96 animate-[aurora-1_20s_ease-in-out_infinite] rounded-full bg-primary/30 opacity-20 blur-3xl filter dark:opacity-50\" />\n          {/* Main Aurora Blob 2 (Secondary/Muted Color) */}\n          <div className=\"absolute -bottom-1/4 right-1/4 h-96 w-96 animate-[aurora-2_20s_ease-in-out_infinite] rounded-full bg-muted-foreground/30 opacity-10 blur-3xl filter dark:opacity-30\" />\n        </div>\n\n        {/* Content */}\n        <div className=\"relative z-10\">{children}</div>\n      </div>\n    </div>\n  );\n};",
        "demoCode": "\"use client\";\n\nimport React from \"react\";\nimport { motion } from \"framer-motion\";\nimport { AuroraHero } from \"@/components/ui/hero-2\"; // Adjust the import path as needed\n\nexport default function AuroraHeroDemo() {\n  return (\n    <AuroraHero>\n      <motion.h1\n        initial={{ opacity: 0, y: 40 }}\n        animate={{ opacity: 1, y: 0 }}\n        transition={{\n          delay: 0.3,\n          duration: 0.8,\n          ease: \"easeInOut\",\n        }}\n        className=\"bg-gradient-to-b from-foreground to-muted-foreground bg-clip-text text-center text-4xl font-bold tracking-tight text-transparent md:text-6xl\"\n      >\n        Experience the Aurora\n      </motion.h1>\n      <motion.p\n        initial={{ opacity: 0, y: 40 }}\n        animate={{ opacity: 1, y: 0 }}\n        transition={{\n          delay: 0.5,\n          duration: 0.8,\n          ease: \"easeInOut\",\n        }}\n        className=\"mx-auto mt-4 max-w-lg text-center text-base text-muted-foreground\"\n      >\n        A stunning, animated background that brings your hero sections to\n        life, adapting beautifully to both light and dark themes.\n      </motion.p>\n    </AuroraHero>\n  );\n}",
        "registryDependencies": {
          "filesWithRegistry": {},
          "npmDependencies": {}
        },
        "isPublic": true,
        "registryTeamId": null,
        "registryUserId": null
      },
      "quota": {
        "tier": "paid",
        "remaining": null,
        "perDay": null,
        "resetAt": null,
        "upgradeUrl": null
      }
    }
  }
}