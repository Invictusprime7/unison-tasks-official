/**
 * Theme utilities — DEPRECATED
 * Re-exports from the canonical theme system for backward compatibility.
 * New code should import from '@/themes' or '@/themes/utils'.
 */

export { themeToCSS, hsl, hsla, containerStyle, sectionStyle, headingStyle, bodyStyle, primaryButtonStyle, outlineButtonStyle, cardStyle } from '@/themes/utils';
