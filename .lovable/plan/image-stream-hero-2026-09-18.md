# Image Stream Hero

## Build
- Preserve the supplied 21st.dev component in the intake archive with provenance and dependency review.
- Adapt the corridor animation into a canonical `hero:image-stream` implementation using industry-provided media, semantic theme tokens, canonical slots/intents, responsive sizing, and reduced-motion behavior.
- Add the matching portable VFS recipe so generated sites render the same hero outside the builder.

## Generation wiring
- Promote it through the automatic Unison registration manifest as a preferred 21st.dev variant.
- Make it eligible for home pages in every art-direction pack, allowing every generated industry to select it without forcing the same hero on every site.
- Add tests covering registration, all-pack availability, source provenance, and emitted/runtime parity.

## Verification
- Run the registration audit, focused policy/parity tests, TypeScript checks, and confirm the preview build remains clean.
