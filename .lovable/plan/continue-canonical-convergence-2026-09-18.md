# Continue Canonical Convergence

## Goal
Complete the next open convergence slice without adding a parallel registry or changing the newer launch policy: contextual composition remains required, Stage 4b remains mandatory, and Lane B remains optional candidate enrichment.

## Scope
1. **Reconcile the governing documentation**
   - Record the newer required-composition rule consistently in the roadmap and canonical plans.
   - Remove contradictory statements that describe AI as directly authoritative or deterministic output as an AI-disabled alternate path.

2. **Close Wizard Registry Context v2**
   - Extend the existing derived Wizard context with bounded project/business assets, approved runtime dependencies, primitive families, capability requirements, and resolved artifact/slot contracts.
   - Derive every field from the existing Asset, Variant, Artifact, Catalog, Intent, and Generated UI Foundation owners; create no new registry.
   - Keep asset projection explicitly scoped to the active business/project and omit unrelated assets.

3. **Wire the bounded context to AI consumers**
   - Pass the relevant projections to the composition planner and Lane B enrichment.
   - Preserve existing payload budgeting and avoid forwarding the unfiltered global registry.

4. **Add closure tests**
   - Prove project asset isolation.
   - Prove dependency, primitive, capability, and artifact/slot fields are derived from canonical declarations.
   - Prove both AI stages receive the bounded context and protected canonical files remain outside AI ownership.

## Technical constraints
- No new runtime registry, VFS writer, theme engine, launcher, or persistence authority.
- No direct AI writes to canonical VFS; accepted changes still pass validation, preflight, and `commitMutation()`.
- Existing saved snapshots remain readable when the new context fields are absent.
- Do not broaden 21st inventory or add editor controls in this batch.

## Verification
- Run focused Wizard registry, composition, Lane B, launch handoff, and snapshot compatibility tests.
- Run TypeScript checks and confirm the preview build is green.
